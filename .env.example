# ให้ Next/Frontend ฟังทุกอินเทอร์เฟซในคอนเทนเนอร์
HOST=0.0.0.0

# พอร์ตที่เซิร์ฟเวอร์ Frontend (Next) ฟังอยู่ "ภายใน" คอนเทนเนอร์
PORT=

# พอร์ต "ข้างนอกเครื่อง/ที่ผู้ใช้เปิดเข้า" (ตัวที่ docker publish)
# และมักใช้ค่าเดียวกับ PORT เพื่อความง่าย
NGINX_PORT=

# URL ของ Backend ที่ Frontend จะ proxy ไปหา
# - รันใน Docker: ใช้ "ชื่อ service" แทน localhost
# “ใส่ /api ต่อท้าย”
NGINX_PROXY=
